# ─── Database ─────────────────────────────────────────────────────
DATABASE_URL="postgresql://growfast:growfast_dev_2024@localhost:5432/growfast_laundry?schema=public"
POSTGRES_USER=growfast
POSTGRES_PASSWORD=growfast_dev_2024
POSTGRES_DB=growfast_laundry

# ─── Backend ──────────────────────────────────────────────────────
PORT=3000
JWT_SECRET=dev-jwt-secret-change-in-production
JWT_EXPIRES_IN=24h

# ─── Frontend ─────────────────────────────────────────────────────
VITE_API_URL=http://localhost:3000/api

# ─── Photo Storage ────────────────────────────────────────────────
PHOTO_STORAGE_PROVIDER=local
PHOTO_UPLOAD_DIR=uploads/photos
PHOTO_MAX_FILE_SIZE=10485760

# ⚠️  WARNING: These are DEVELOPMENT-ONLY values.
# ⚠️  NEVER use these credentials in production.
# ⚠️  NEVER commit the .env file to version control.
